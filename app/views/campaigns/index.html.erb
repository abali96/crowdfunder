<h1> Discover Campaigns </h1>
<% @campaigns.each do |c| %>
  <h3><%= link_to c.name, campaign_path(c) %></h3>
  <p>Tags:  <%= raw c.tag_list.map { |t| link_to t, tag_path(t) }.join(', ')%></p>
<% end %>