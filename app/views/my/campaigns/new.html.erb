<h1>New Campaign</h1>
<div class="well">
 <%= link_to [:my, :campaigns], :class => 'btn btn-small' do %>
 &laquo; My Campaigns
 <% end %>
</div>

<%= form_for [:my, @campaign] do |f| %>
  <%= render f %>
 <div class="form-actions">
 <%= f.submit 'Publish Campaign', class: 'btn' %>
 </div>
<% end %>